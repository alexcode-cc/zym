# Git Commit Message Conventions

## 提交訊息規範

本專案遵循 **AngularJS Git Commit Message Conventions** 規範，所有提交訊息必須使用**繁體中文**撰寫。

### 提交訊息格式

```
<類型>(<範圍>): <主題>

<正文>

<頁腳>
```

### 標題組成

- **類型（type）**：描述提交的類型，必填
- **範圍（scope）**：描述提交影響的範圍，選填
- **主題（subject）**：簡要描述提交的內容，必填

### 允許的類型（type）

- `feat`：新增功能
- `fix`：修復錯誤
- `docs`：僅修改文件
- `style`：格式變更（不影響程式碼運行的變更，如空格、格式化、缺少分號等）
- `refactor`：重構（既不是新增功能，也不是修復錯誤的代碼變更）
- `perf`：性能優化
- `test`：新增或修改測試
- `build`：構建系統或外部依賴的變更
- `ci`：CI 配置文件和腳本的變更
- `chore`：其他不修改 src 或測試文件的變更（如更新構建工具、依賴管理等）

### 範圍（scope）

範圍用來描述提交影響的模組或功能區域，例如：`核心`、`模組`、`API`、`構建`、`配置` 等。

### 主題（subject）

- 用簡潔的語句描述變更內容
- 使用祈使句，現在時態，例如：「修正錯誤」而非「修正了錯誤」或「已修正錯誤」
- 首字母不大寫，句末不加句號
- 長度建議不超過 50 個字元

### 正文（body）

- 詳細描述變更的動機和與先前行為的對比
- 使用祈使句，現在時態
- 每行不超過 72 個字元
- 可以包含多個段落，段落之間用空行分隔

### 頁腳（footer）

- 列出不兼容的變更，並說明變更內容和遷移方法
- 關閉的問題，例如：`Closes #123`、`Fixes #456`
- 重大變更（BREAKING CHANGE）說明

### 範例

#### 新增功能
```
feat(核心): 新增用戶認證功能

新增基於 JWT 的用戶認證機制，支援登入、註冊和登出功能。

Closes #123
```

#### 修復錯誤
```
fix(模組): 修正數據加載錯誤

修正因 API 變更導致的數據加載錯誤，確保數據正確顯示。

Fixes #456
```

#### 文檔更新
```
docs(API): 更新 API 使用說明

新增新功能的使用說明，並修正舊版本的錯誤描述。
```

#### 重構
```
refactor(核心): 重構用戶服務

重構用戶服務代碼，提升可讀性和維護性，無功能變更。
```

#### 性能優化
```
perf(渲染器): 優化列表渲染性能

使用虛擬滾動技術優化長列表渲染，減少 DOM 節點數量。
```

#### 測試
```
test(模組): 新增用戶認證測試

為用戶認證功能新增單元測試，確保邏輯正確性。
```

#### 構建配置
```
chore(構建): 更新 npm 配置

移除 pnpm 專用配置，添加 legacy-peer-deps 以解決依賴衝突。
```

#### 重大變更
```
feat(API): 重構用戶 API 介面

BREAKING CHANGE: 用戶 API 端點從 /api/users 變更為 /api/v2/users
請更新所有相關的 API 調用。
```

### 重要提醒

1. **所有提交訊息必須使用繁體中文**
2. **嚴格遵循上述格式規範**
3. **類型必須從允許的類型列表中選擇**
4. **主題必須簡潔明確，使用祈使句**
5. **重大變更必須在頁腳中明確標註**

